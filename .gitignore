# Build artifacts
Navigator.bin
makeicon
Navigator.iconset/
build.log
run.log
run.pid
*.o
.DS_Store

# Never commit signing keys
*.p12
*.key
navcert.*
ext.entitlements
.build

# The release artifact. It is attached to each GitHub release, which is where a
# binary belongs — tracking it put 514 MB of dead weight into this history (90% of
# the repo) before it was purged. Never commit it again.
Navigator.zip
